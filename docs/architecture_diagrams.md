# CDCP AI - Architecture Diagrams

## 1. System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        CDCP AI SYSTEM                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐              ┌──────────────────────────────┐ │
│  │   Frontend   │              │      Backend (FastAPI)       │ │
│  │   (React)    │◄────────────►│         Port 8001            │ │
│  │  Port 4200   │   HTTP/REST  │                              │ │
│  └──────────────┘              └──────────────────────────────┘ │
│                                             │                     │
│                                             ▼                     │
│                         ┌──────────────────────────────┐         │
│                         │   7 API Controllers          │         │
│                         ├──────────────────────────────┤         │
│                         │ • health_controller          │         │
│                         │ • cdcp_controller (queries)  │         │
│                         │ • rag_controller (RAG ops)   │         │
│                         │ • asr_controller (speech)    │         │
│                         │ • tts_controller (voice)     │         │
│                         │ • agent_controller           │         │
│                         │ • voice_controller (full)    │         │
│                         └──────────────────────────────┘         │
│                                     │                             │
│                    ┌────────────────┼────────────────┐           │
│                    ▼                ▼                ▼            │
│         ┌──────────────┐  ┌─────────────┐  ┌──────────────┐    │
│         │  AI Services │  │   RAG       │  │  LangGraph   │    │
│         │              │  │  Pipeline   │  │    Agent     │    │
│         │ • ASR        │  │             │  │              │    │
│         │ • TTS        │  │ • Scraper   │  │ • 6 Tools    │    │
│         │ • LLM        │  │ • Embedder  │  │ • Coordinator│    │
│         │ • Supervisor │  │ • VectorDB  │  │ • Memory     │    │
│         └──────────────┘  └─────────────┘  └──────────────┘    │
│                                     │                             │
│                                     ▼                             │
│                         ┌──────────────────────┐                 │
│                         │   External Services  │                 │
│                         ├──────────────────────┤                 │
│                         │ • ChromaDB (local)   │                 │
│                         │ • OpenAI API         │                 │
│                         │ • Whisper Models     │                 │
│                         │ • Transformer Models │                 │
│                         └──────────────────────┘                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. Voice-to-Voice Workflow (Main Feature)

```
┌─────────────────────────────────────────────────────────────────────┐
│                    VOICE CONVERSATION PIPELINE                       │
└─────────────────────────────────────────────────────────────────────┘

   User
    │
    │ 🎤 Audio File (WAV/MP3)
    ▼
┌──────────────────┐
│ POST /voice/     │
│  conversation    │
└────────┬─────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    LANGGRAPH INTELLIGENT AGENT                       │
│                                                                       │
│  ┌─────────────────────────────────────────────────────────┐        │
│  │                    COORDINATOR                           │        │
│  │  (Decides which tool to call based on state)            │        │
│  └──────────────────────┬──────────────────────────────────┘        │
│                         │                                             │
│      ┌──────────────────┼──────────────────┬──────────────┐         │
│      ▼                  ▼                  ▼              ▼          │
│  ┌────────┐      ┌──────────┐      ┌──────────┐   ┌──────────┐    │
│  │ Tool 1 │      │  Tool 2  │      │  Tool 3  │   │  Tool 4  │    │
│  │ ASR    │      │   RAG    │      │ Evaluate │   │  OpenAI  │    │
│  └───┬────┘      └────┬─────┘      └────┬─────┘   └────┬─────┘    │
│      │                │                 │              │             │
│      │  ┌──────────┐  │  ┌──────────┐  │              │             │
│      └─►│  Tool 5  │◄─┘  │  Tool 6  │  │              │             │
│         │ Fallback │     │   TTS    │◄─┴──────────────┘             │
│         └──────────┘     └────┬─────┘                                │
└───────────────────────────────┼──────────────────────────────────────┘
                                │
                                ▼
                          🔊 Audio Response


Detailed Steps:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Step 1: TRANSCRIBE_AUDIO
   Input: Base64 audio data
   Process: Whisper model converts to text
   Output: "TRANSCRIPT: What is CDCP?"
   ────────────────────────────────────────▶

Step 2: RAG_SEARCH_AND_GENERATE
   Input: User query text
   Process:
     • Embed query → [0.123, 0.456, ...]
     • Search ChromaDB → Top 3 docs
     • Build context from docs
     • Fine-tuned model generates answer
   Output: "RAG_RESULT: {answer, context, query}"
   ────────────────────────────────────────▶

Step 3: EVALUATE_ANSWER
   Input: Query + Answer + Context
   Process: OpenAI GPT-4 evaluates quality
   Output: "EVALUATE_RESULT: {is_good: true/false, reason: '...'}"
   ────────────────────────────────────────▶

Step 4a: If GOOD → SYNTHESIZE_SPEECH
   Input: Answer text
   Process: TTS engine generates audio
   Output: "AUDIO_FILE: /tmp/audio_xyz.wav"
   ────────────────────────────────────────▶ DONE ✓

Step 4b: If BAD → GENERATE_WITH_OPENAI
   Input: Original query
   Process: GPT-4 generates better answer
   Output: "OPENAI_RESULT: {answer, source: 'openai'}"
   ────────────────────────────────────────▶

Step 5: SYNTHESIZE_SPEECH (after OpenAI)
   Input: OpenAI answer text
   Process: TTS engine generates audio
   Output: "AUDIO_FILE: /tmp/audio_xyz.wav"
   ────────────────────────────────────────▶ DONE ✓
```

---

## 3. RAG Pipeline Details

```
┌────────────────────────────────────────────────────────────────────┐
│                     RAG PIPELINE ARCHITECTURE                       │
└────────────────────────────────────────────────────────────────────┘

PHASE 1: INGESTION (Setup / One-time)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  POST /rag/ingest
       │
       ▼
  ┌─────────────────┐
  │  Web Scraper    │  Crawls CDCP websites
  │  Service        │  • canada.ca/dental-care-plan
  └────────┬────────┘  • Max pages: configurable
           │
           │ Documents [title, content, url, metadata]
           ▼
  ┌─────────────────┐
  │  Document       │  Chunks into 512-token pieces
  │  Chunker        │  • Overlap: 50 tokens
  └────────┬────────┘  • Preserves context
           │
           │ Chunks [content, title, url, section]
           ▼
  ┌─────────────────┐
  │  Embedding      │  Sentence Transformers
  │  Service        │  • all-MiniLM-L6-v2
  └────────┬────────┘  • 384-dim vectors
           │
           │ Embeddings [[0.12, 0.45, ..., 0.89], ...]
           ▼
  ┌─────────────────┐
  │  Vector DB      │  ChromaDB (Persistent)
  │  (ChromaDB)     │  • Collection: cdcp_documents
  └─────────────────┘  • Location: ./chroma_db


PHASE 2: SEARCH (Real-time)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  User Query: "How do I apply for CDCP?"
       │
       ▼
  ┌─────────────────┐
  │  Embed Query    │  Same model as documents
  │                 │  → [0.23, 0.67, ..., 0.91]
  └────────┬────────┘
           │
           ▼
  ┌─────────────────┐
  │  Vector Search  │  Cosine similarity
  │  (ChromaDB)     │  • n_results: 3
  └────────┬────────┘  • Returns top matches
           │
           │ Top 3 Chunks with similarity scores
           ▼
  ┌─────────────────────────────────────────┐
  │  Results:                                │
  │  1. "To apply visit..." (95% match)      │
  │  2. "Eligibility requires..." (89%)      │
  │  3. "Application process..." (87%)       │
  └─────────────────────────────────────────┘
           │
           ▼
  ┌─────────────────┐
  │  Fine-tuned     │  Generates answer using context
  │  LLM + RAG      │  • Context-aware
  └────────┬────────┘  • Domain-specific
           │
           ▼
     Final Answer
```

---

## 4. Supervised RAG (Quality Control)

```
┌──────────────────────────────────────────────────────────────────┐
│              SUPERVISED RAG WITH QUALITY EVALUATION               │
└──────────────────────────────────────────────────────────────────┘

                        Query: "Am I eligible?"
                                  │
                                  ▼
        ┌─────────────────────────────────────────────┐
        │        RAG Search + Fine-tuned Model        │
        └───────────────────┬─────────────────────────┘
                            │
                            ▼
        ┌──────────────────────────────────────────────────┐
        │  Generated Answer:                               │
        │  "You may be eligible if you meet income         │
        │   requirements and don't have coverage..."       │
        └───────────────────┬──────────────────────────────┘
                            │
                            ▼
        ┌─────────────────────────────────────────────────┐
        │         OPENAI SUPERVISOR (GPT-4)               │
        │  Evaluates:                                     │
        │  ✓ Accuracy    ✓ Relevance    ✓ Completeness  │
        └───────────────────┬─────────────────────────────┘
                            │
                ┌───────────┴──────────┐
                │                      │
                ▼                      ▼
         ┌────────────┐         ┌────────────┐
         │   GOOD ✓   │         │   BAD ✗    │
         └──────┬─────┘         └──────┬─────┘
                │                      │
                │                      ▼
                │            ┌──────────────────┐
                │            │  GPT-4 Fallback  │
                │            │  (Better Answer) │
                │            └────────┬─────────┘
                │                     │
                └─────────┬───────────┘
                          ▼
                    ┌──────────┐
                    │   TTS    │
                    └──────────┘
                          │
                          ▼
                    Voice Response


Example Evaluation:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Query: "How much does CDCP cost?"
RAG Answer: "CDCP provides coverage for dental services."
           ❌ Not answering the question

Evaluation Result:
{
  "evaluation": "BAD",
  "is_good": false,
  "reason": "Answer does not address the cost question.
            It only states what CDCP covers, not pricing."
}

Action: → Call GPT-4 fallback
GPT-4 Answer: "The Canadian Dental Care Plan has no
              premiums, but may require co-payments
              based on your family income..."
           ✅ Accurate and complete

Final Output: GPT-4 answer → TTS → User
```

---

## 5. LangGraph Agent State Machine

```
┌────────────────────────────────────────────────────────────────┐
│                 LANGGRAPH STATE MACHINE                         │
└────────────────────────────────────────────────────────────────┘

     START
       │
       ▼
  ┌──────────────┐
  │ COORDINATOR  │◄──────────┐
  │   (Brain)    │            │
  └───┬──────────┘            │
      │                       │
      │ Decides next action   │
      ▼                       │
  ┌──────────────┐            │
  │  TOOL NODE   │────────────┘
  │  (Executor)  │  Returns result
  └───┬──────────┘
      │
      │ Tool completed?
      ▼
  ┌──────────────┐
  │ Should       │
  │ Continue?    │
  └───┬──────────┘
      │
      ├─ Yes → Back to COORDINATOR
      │
      └─ No → END


State Flow Example:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Messages: [HumanMessage("AUDIO_DATA:...")]
  ↓
Coordinator: "I see audio, call transcribe_audio"
  ↓
Messages: [..., AIMessage(tool_calls=[transcribe_audio])]
  ↓
ToolNode: Execute transcribe_audio
  ↓
Messages: [..., ToolMessage("TRANSCRIPT: What is CDCP?")]
  ↓
Coordinator: "I see transcript, call rag_search_and_generate"
  ↓
Messages: [..., AIMessage(tool_calls=[rag_search_and_generate])]
  ↓
ToolNode: Execute rag_search_and_generate
  ↓
Messages: [..., ToolMessage("RAG_RESULT: {...}")]
  ↓
Coordinator: "I see RAG result, call evaluate_answer"
  ↓
Messages: [..., AIMessage(tool_calls=[evaluate_answer])]
  ↓
ToolNode: Execute evaluate_answer
  ↓
Messages: [..., ToolMessage("EVALUATE_RESULT: {is_good: true}")]
  ↓
Coordinator: "Answer is good, call synthesize_speech"
  ↓
Messages: [..., AIMessage(tool_calls=[synthesize_speech])]
  ↓
ToolNode: Execute synthesize_speech
  ↓
Messages: [..., ToolMessage("AUDIO_FILE: /tmp/...")]
  ↓
Coordinator: "Audio file created, done!"
  ↓
Should Continue: "NO - AUDIO_FILE found"
  ↓
END

Final State:
{
  "messages": [
    HumanMessage(...),
    AIMessage(tool_calls=[...]),
    ToolMessage("TRANSCRIPT: ..."),
    AIMessage(tool_calls=[...]),
    ToolMessage("RAG_RESULT: ..."),
    AIMessage(tool_calls=[...]),
    ToolMessage("EVALUATE_RESULT: ..."),
    AIMessage(tool_calls=[...]),
    ToolMessage("AUDIO_FILE: ...")
  ]
}
```

---

## 6. Deployment Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│                   KUBERNETES DEPLOYMENT                           │
└──────────────────────────────────────────────────────────────────┘

                     ┌─────────────────┐
                     │   Load Balancer │
                     │   (NodePort)    │
                     └────────┬────────┘
                              │
                 ┌────────────┴─────────────┐
                 │                          │
                 ▼                          ▼
        ┌────────────────┐         ┌────────────────┐
        │  API Service   │         │   UI Service   │
        │  Port: 8001    │         │   Port: 4200   │
        └────────┬───────┘         └────────┬───────┘
                 │                          │
      ┌──────────┴──────────┐    ┌─────────┴────────┐
      ▼                     ▼    ▼                  ▼
┌──────────┐         ┌──────────┐  ┌──────────┐ ┌──────────┐
│ API Pod 1│         │ API Pod 2│  │ UI Pod 1 │ │ UI Pod 2 │
└──────────┘         └──────────┘  └──────────┘ └──────────┘
     │                     │
     └──────────┬──────────┘
                ▼
      ┌──────────────────┐
      │  Persistent Vol  │
      │  (ChromaDB)      │
      │  ./chroma_db     │
      └──────────────────┘


Docker Architecture:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Container 1: cdcp-api
┌────────────────────────────┐
│ FROM python:3.10-slim      │
│                            │
│ • FastAPI app              │
│ • Whisper models           │
│ • Sentence Transformers    │
│ • ChromaDB                 │
│ • LangGraph                │
│                            │
│ EXPOSE 8001                │
│ CMD uvicorn main:app       │
└────────────────────────────┘

Container 2: cdcp-ui
┌────────────────────────────┐
│ FROM node:18-alpine        │
│                            │
│ • React app                │
│ • TypeScript               │
│ • Build artifacts          │
│                            │
│ EXPOSE 4200                │
│ CMD npm start              │
└────────────────────────────┘

Volume Mount:
  • ./chroma_db → /app/chroma_db
  • Persists vector database across restarts
```

---

## 7. Data Flow Diagram

```
┌────────────────────────────────────────────────────────────────────┐
│                         DATA FLOW                                   │
└────────────────────────────────────────────────────────────────────┘

INPUT STAGE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Audio File (Binary)
     │ Base64 encode
     ▼
"AUDIO_DATA:iVBORw0KGgoAAAANS..."
     │ LangGraph message
     ▼
HumanMessage(content="AUDIO_DATA:...")


PROCESSING STAGE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Tool: transcribe_audio
     │ Whisper decode
     ▼
"What is the Canadian Dental Care Plan?"
     │ ToolMessage
     ▼
ToolMessage(content="TRANSCRIPT: What is...")


Tool: rag_search_and_generate
     │ Embed query
     ▼
[0.234, 0.567, 0.123, ..., 0.891]  (384 dims)
     │ ChromaDB search
     ▼
[
  {content: "CDCP is a program...", score: 0.95},
  {content: "To be eligible...", score: 0.89},
  {content: "Coverage includes...", score: 0.87}
]
     │ Build context string
     ▼
"[Document 1] (Relevance: 95.0%)
Title: CDCP Overview
Content: CDCP is a program..."
     │ Fine-tuned LLM generate
     ▼
{
  "query": "What is...",
  "answer": "The Canadian Dental Care Plan...",
  "context": "..."
}
     │ JSON stringify
     ▼
ToolMessage(content="RAG_RESULT: {...}")


Tool: evaluate_answer
     │ OpenAI GPT-4 evaluate
     ▼
{
  "evaluation": "GOOD",
  "is_good": true,
  "reason": "Answer is accurate and complete"
}
     │ JSON stringify
     ▼
ToolMessage(content="EVALUATE_RESULT: {...}")


Tool: synthesize_speech
     │ TTS engine
     ▼
WAV Audio Bytes (16-bit PCM, 22050 Hz)
     │ Write to temp file
     ▼
"/tmp/tmpXYZ123.wav"
     │ ToolMessage
     ▼
ToolMessage(content="AUDIO_FILE: /tmp/tmpXYZ123.wav")


OUTPUT STAGE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Extract from messages:
  • transcription: "What is..."
  • response_text: "The Canadian Dental Care Plan..."
  • audio_path: "/tmp/tmpXYZ123.wav"
     │ Read audio file
     ▼
Audio Bytes
     │ Base64 encode
     ▼
Response JSON:
{
  "transcription": "What is...",
  "answer": "The Canadian Dental Care Plan...",
  "audio_base64": "iVBORw0KGgo..."
}
     │ HTTP Response
     ▼
Frontend plays audio
```

---

## 8. Technology Stack Layers

```
┌────────────────────────────────────────────────────────────────┐
│                    TECHNOLOGY STACK                             │
└────────────────────────────────────────────────────────────────┘

LAYER 7: DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Kubernetes    Docker    Docker Compose    Minikube
     │             │            │               │
     └─────────────┴────────────┴───────────────┘

LAYER 6: ORCHESTRATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  LangGraph Agent      MemorySaver      StateGraph
     │                     │                │
     └─────────────────────┴────────────────┘

LAYER 5: AI/ML SERVICES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Whisper ASR    Fine-tuned LLM    OpenAI GPT-4    TTS Engine
     │                │                 │               │
     └────────────────┴─────────────────┴───────────────┘

LAYER 4: DATA LAYER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ChromaDB      Sentence Transformers    Vector Embeddings
     │                   │                      │
     └───────────────────┴──────────────────────┘

LAYER 3: BUSINESS LOGIC
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  RAG Service   ASR Service   TTS Service   LLM Service
     │              │             │              │
     └──────────────┴─────────────┴──────────────┘

LAYER 2: API LAYER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  FastAPI    Pydantic    CORS Middleware    Uvicorn
     │          │              │                │
     └──────────┴──────────────┴────────────────┘

LAYER 1: FRONTEND
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  React    TypeScript    React Router    Webpack
     │         │              │             │
     └─────────┴──────────────┴─────────────┘

LAYER 0: INFRASTRUCTURE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Python 3.10+    Node.js 18+    CUDA (Optional)    Linux/macOS
```

---

## Summary

These diagrams show:
1. **System Architecture** - Overall component structure
2. **Voice-to-Voice Workflow** - Main feature end-to-end
3. **RAG Pipeline** - Knowledge base ingestion & search
4. **Supervised RAG** - Quality control mechanism
5. **LangGraph State Machine** - Agent decision-making
6. **Deployment Architecture** - K8s & Docker setup
7. **Data Flow** - How data transforms through pipeline
8. **Technology Stack** - All technologies used in layers

Use these diagrams during your demo to explain different aspects of the system!
